#include <bits/stdc++.h>

using namespace std;
using ll = long long;
using ld = long double;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout << fixed << setprecision(10);

    double pi = M_PI;
    int r;
    cin >> r;

    cout << r*r*pi <<  '\n' << 2.0*r*r;

    return 0;
}